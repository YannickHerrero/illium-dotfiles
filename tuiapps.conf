# tuiapps.conf — TUI apps available in dmenu (launched inside st)
# Format: Name=command (one per line, lines starting with # are ignored)

Bluetooth=bluetuith
WiFi=nmtui
Audio Mixer=pulsemixer
System Monitor=btop
File Manager=yazi
rstools=rstools
